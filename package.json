{
  "name": "clock",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "tsc && vite build && vite preview",
    "deploy": "tsc && vite build && gh-pages -d dist && rm -rf ./dist"
  },
  "dependencies": {
    "axios": "^0.27.2",
    "moment-timezone": "^0.5.34",
    "preact": "^10.9.0"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.3.0",
    "gh-pages": "^4.0.0",
    "sass": "^1.54.0",
    "typescript": "^4.6.4",
    "vite": "^3.0.0"
  }
}
